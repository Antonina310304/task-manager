import { ViewLink } from '../types';

const viewBtn: Record<string, ViewLink> = {
  default: 'default',
  delete: 'delete',
  icon: 'icon',
  nav: 'nav',
  primary: 'primary',
  secondary: 'secondary',
};

export default viewBtn;
